import 'package:flutter/material.dart' show BoxShadow, Colors, BlurStyle, Offset;

class Styles {
  static BoxShadow boxShadow = const BoxShadow(
    color: Colors.black26,
    blurRadius: 10,
    spreadRadius: 1,
    offset: Offset(0, 10),
  );
}
